@model IEnumerable<FinalProject.Models.Bookmark>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateAdded)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Member)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Book)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateAdded)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Member.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Book.Isbn)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BookmarkId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BookmarkId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BookmarkId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
